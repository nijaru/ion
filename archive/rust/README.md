# Archived Rust Implementation

This directory contains the archived Rust implementation and Rust-TUI design material that previously drove `ion`.

Key points:

- The preserved historical checkpoint is the Git tag `stable-rnk`.
- That tag points to the last known stable RNK-era mainline before the Go/Bubble Tea rewrite became the active direction.
- The contents here are reference material, not the active product path.
