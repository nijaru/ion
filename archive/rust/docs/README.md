# Archived Rust Docs

These docs were kept because they still contain useful historical context, design rationale, or implementation detail from the Rust-era `ion` architecture.

Use them for:

- understanding why the rewrite happened
- recovering ideas or implementation details
- comparing old and new host/runtime approaches

Do not treat them as active implementation guidance for `main`.
