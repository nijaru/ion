# Ion roadmap

This roadmap delivers [DESIGN.md](DESIGN.md) and [TERMINAL.md](TERMINAL.md). It deliberately prioritizes a correct, cohesive core over preserving the current implementation.

Long-term/project memory, knowledge stores, shared task boards, vector stores and planner layers are outside the core roadmap. They may be evaluated later against a stable baseline and must not shape the core prematurely.

## Current position

| Deliverable | State | Evidence |
|---|---|---|
| Core architecture | K0–K5 foundations and part of K6 implemented; correctness repair gate active | DESIGN.md; repair order below; docs/core-runtime-migration.md |
| Worker/fork/session topology | Retained spawn, archive, worker-local turns and closure receipts implemented; joined-run expansion paused behind repairs and the coding baseline | K6 below; repair order below |
| Task execution/recovery | K2 lifecycle boundary plus K3 driver implemented, including waits, capacity, close, interruption/uncertainty, finalization, turns and readiness dispatch | R0.1/R0.3; `crates/ion-core/src/task/`; `crates/ion-core/src/session/scheduler.rs`; `crates/ion-core/tests/k5_chain.rs` |
| IDs/order | Session-local typed sequence implemented, including SQLite representation | R0.4; K1/K2/K4 |
| AI boundary | Independent provider-neutral `ion-ai` contract crate implemented | R0.5; `crates/ion-ai/` |
| Storage topology/engine | Per-session SQLite schema v5 with exclusive ownership implemented; bounded residency remains open; process-death evidence is limited to acknowledged entry commits | `crates/ion-core/src/store/sqlite/`; `crates/ion-core/tests/k4_sqlite.rs` |
| Fresh production core | Legacy runtime removed; scripted generation/tool chain works; no real-provider coding loop yet | cleanup commit `2d273f78`; evidence log below |
| TUI/group target | Interaction requirements drafted; fresh application/TUI not yet rebuilt | TERMINAL.md |

R0 was validated at `81c344d713f13b73e19232b4ad36dfe40ba663b9` by CI run `34718467220`. The old core was then physically removed instead of retained as a compatibility runtime. The K3 core-driver checkpoint `a33e0fb22073857cc724244b23f99e0c12249147` (CI run `34725190754`) remains historical validation evidence; later commits are listed in the evidence log.

The design remains reopenable if production evidence disproves a contract. Do not preserve an implementation merely because it has now been rewritten once.

## 1. Immediate work: repair, then a real coding baseline

The 2026-09-13 source review at `888d103c` preserves the core architecture but supersedes the previous “joined workers next” order. The original findings were source-derived failure scenarios; subsequent repair rows and the 2026-09-14 review distinguish executed reproductions from remaining source-derived gaps. The existing green suite does not close them. Start each repair with a regression test at the affected boundary; record the fixing commit and actual evidence here when it closes. Do not infer closure from a nearby test or from updating the design.

### Repair and delivery order

**R1 is closed at `5bf3ea87`, R2 at `b4730bc8`, R3 at `9c19a331` (`8c91cb28` adds the process-death witness), R4 at `d6fad545` (`aba6b92c` repairs the review's provenance findings) and R5 at `fa991317`**; R6–R8 and R12 are open; R9 closed at `682307e2`, R10 at `6618e1b1` and R11 at `e6f5de83`. This table is the work-order authority; the K/P sections retain subsystem scope, not a competing execution sequence.

| Order | Observable behavior / semantic owner | Failure boundary and acceptance evidence |
|---|---|---|
| R1 — exclusive writable ownership | **Closed `5bf3ea87`.** The store holds a kernel-held lock before any SQLite open, schema check or reconstruction, and releases it last in close; cursor CAS remains defense-in-depth; load reads one transaction | Refusal is `SessionError::SessionInUse` and happens before reconstruction, so a refused process observes and changes nothing. A child process is refused while the parent lives and commits after the parent releases. Graceful and fault close both release ownership. `k4_ownership.rs` also asserts, against a concurrent raw writer, that no visible entry is newer than the committed cursor. |
| R2 — durable turn control | **Closed `b4730bc8`.** The turn's cancellation barrier is durable turn state on the conversation holding the slot, set with the member marks and cleared with the slot release, so it no longer depends on the root operation's terminal state; `turn_closed_by` denotes closure provenance, not the answer | `k3_turns.rs` settles the root, keeps a member live, cancels, and asserts the cleanup successor the member plans is born cancelled; it also pins that a released turn's barrier does not reach a later turn. `k4_sqlite.rs` pins that the barrier survives close and reopen. Terminal operation outcomes are still never rewritten. |
| R3 — fail-closed recovery | **Closed `9c19a331`; process-death witness `8c91cb28`.** Checkpoint decoding is three-way (absent / readable / unreadable) in `builtin/checkpoint.rs`, and both built-ins fail closed on the third: generation settles Indeterminate without rebuilding or dispatching, a tool call is never repeated and never reported as known. Tool resolution reads evidence before the catalogue, and the dispatch record freezes the recovery policy in force at hand-over, so a removed, replaced or newly retry-safe tool cannot resolve an earlier call | `k5_recovery.rs`: 5 of 9 cases fail against pre-repair `1c31ea49` (Completed for a removed or newly retry-safe tool; an unreadable request rebuilt and retried without ever settling) and all pass after. A real process death inside the dispatch window with an external file witness lands the effect once and is recovered as Indeterminate without repeating it; an intact checkpoint is still replayed; a checkpoint column that is not JSON refuses reconstruction. Prepared tool evidence names the call and the recovery policy, not the implementation that served it, so a same-name implementation replaced between dispatch and recovery is not distinguished; recording implementation identity (or an explicit compatibility decision) and a reopen-after-replacement regression remain open, and the protection is policy-level until then. |
| R4 — input placement and answer attempts | **Closed `d6fad545`, provenance repaired at `aba6b92c`.** The session writer appends the user entry and records its placement in the commit that creates the answering turn (admission that starts a turn, or the queued-input binding that starts a successor turn), before any invocation runs; a still-queued input is not placed. `InputDisposition` is `{Queued, Placed{entry, turn}, Abandoned{entry, turn}, Cancelled}`, consumption is attempt evidence in the frozen request, and `retry_input`/`abandon_input` are the explicit attempt commands | `k5_placement.rs`: cancelling before the first token still places exactly one entry and calls no provider; an interrupted attempt places once across a handover; an explicit retry after reopen adds no second user entry, rebinds the same entry to a new attempt and preserves the old closure receipt; abandonment keeps the placement; a retired conversation refuses a retry. Against the pre-repair tree the central claim fails: a cancelled submit leaves 0 user entries. `k6_retirement.rs` pins the retirement cancellation's durability (live vs reopened snapshot). An independent review of the first commit found that a retry could claim to answer an input an edit had omitted and that the frozen provenance used a cutoff comparison; `aba6b92c` derives provenance from the projection's contribution set, drops the context precondition from retry and abandonment, and adds the omitted-input, abandonment, live-member and preserved-outcome cases. |
| R5 — restart-safe observations | **Closed `fa991317`.** The session records `coverage_start`, the earliest commit it can serve a delta for: `None` while it observes its own commits, and the loaded commit on open, so a cursor that predates a restart resnapshots and one at the loaded commit resumes streaming | `k4_sqlite.rs`: commit, retain the cursor, commit again, close, reopen, request from the old cursor and require a resnapshot; the cursor at the loaded commit is served; a reconnected watcher wakes on the next commit and that commit is in the delta from its cursor. `k4_reads.rs` already pins future/unknown cursors and evicted coverage. |
| R6 — bounded storage/output | **In progress.** Slice 1 landed (per-conversation entry index; a plain append no longer materializes the transcript it appends to): append p50 213us->58us at 2k entries and 1519us->129us at 8k, per-turn admission 900us->197us, 8000-turn throughput 696->1650 turns/sec. Slice 2 landed (a page of a transcript costs the page, not the whole transcript): a complete paged read of the same transcript went 46.36ms -> 1.08ms at 4000 entries and 923.7ms -> 4.24ms at 16000, with per-page cost flat at ~17us instead of growing with transcript length. Slice 3 landed (a command prepares its writes in place through a rollback journal instead of copying resident state) together with the task-side indexes the overlay made profitable: 16000-turn build 33.98s -> 3.6s (~800 -> ~4200 turns/sec), admission p50 584us -> 85us and settlement p50 1081us -> 111us, both flat across a 4x history; a plain append costs ~100us at 256 and at 8000 entries. Remaining: bounded residency/indexed reads replacing full-history hydration at open (open still 40ms and RSS still doubles at 16000 entries), frozen-request payload duplication, every byte cap and its failure modes, and a fork page whose cost grows with its ancestor's visible id history. | Measure 100k terminal tasks, deep forks, large output and a tiny active set: RSS, query work, commit latency, bytes/WAL growth, restart and cancellation latency. Test byte caps, disk exhaustion and artifact publication. Bounded return sizes alone do not establish bounded work. Also open, and identified after the first two slices: a fork's page cost is bounded by its page plus *its ancestor's whole visible id history* (ids added after the fork cutoff included), not by depth; `Session::summary` computes counts by scanning every task and `Session::snapshot` clones every record, so ordinary clients need bounded views before they depend on either; generation captures its transcript cutoff *after* paging, and the page read takes only `after`/`limit`, so a finite read boundary must be captured before reconstruction. |
| R7 — thin real coding loop | Conversation configuration/request assembly, provider adapter and execution environment support instructions, selected model/tools, read/edit/exec and minimum permissions | Run an externally verified edit/test task through a headless client. Exercise fixtures from two materially different provider APIs for instructions, controls, replay, ordered content, usage and typed errors; at least one live provider path. Cancel during connection establishment as well as streaming. Freeze resolved configuration, never credentials. Before surface freeze, resolve provider-design §10 and demonstrate typed authoring parity for scoped reads and late registration through the existing adapter (Rust surface review finding 5). Design: [`docs/design/provider-request-assembly.md`](docs/design/provider-request-assembly.md) (proposed, reviewed before implementation; two wire adapters are OpenRouter chat-completions and Anthropic Messages per D15). |
| R8 — usable baseline and evaluation | Built-ins own compaction/context policy and configurable step/cost/deadline limits; clients receive invocation-addressed provisional output | Small coding regression set with externally checked outcomes, repeatable model/configuration, cost/latency and failure records. Test streaming/resume/fork, stale frames, output floods, compaction safety and budget exhaustion. Unknown usage must not be treated as zero. A terminal surface additionally needs reducer/PTY and smoke checks. |

| Order | Observable behavior / semantic owner | Failure boundary and acceptance evidence |
|---|---|---|
| R9 — cancellation owns provider opening | **Closed `1d370462`, completed `682307e2`.** The generation invocation selects cancellation against one owned open-and-collect future with cancellation polled first, and abort reads the durable checkpoint instead of assuming the attempt never ran | `k5_provider_cancellation.rs`, watched failing against the pre-repair handler (4 of 5 cases). A barrier-controlled open future or stream is dropped by a committed cancellation, the old invocation joins, and the fresh abort generation settles with no assistant entry and no tool child; abort reports the prepared attempt count, or settles Indeterminate when the attempt evidence is unreadable rather than reading damage as "never dispatched"; a durable mark before dispatch means no provider open at all; and a settled answer is not rewritten by a later cancellation. Fault close and graceful close keep their existing cooperative-close limits. Deadline ownership remains R7 work at this boundary. |
| R10 — plan handle identity and failure atomicity | **Closed `6618e1b1`.** `TaskPlan` is no longer `Clone`, so a handle cannot be copied into a second branch, and insertion owners reject a duplicate before writing, so a rejected record is never replaced. Reconstruction validates the whole loaded state before returning a writable owner | The diverged-clone witness is now unrepresentable rather than merely rejected: the mutable builder cannot be copied, and external tests did not rely on that latent capability. `session::tests::a_duplicate_insert_leaves_records_and_indexes_unchanged` compares the whole `SessionState` (private indexes included) after duplicate entry, task and input inserts, and fails against the previous `map.insert(..).is_some()` owners, which replaced the record they rejected. `k4_sqlite::an_inconsistent_store_is_refused_rather_than_repaired` covers eight stored fixtures — lowered sequence, running without an invocation, terminal with one, broken ownership reciprocity in both directions, a dependency on a missing task, a forward dependency and an entry whose conversation is gone — each refused with its named rule, while the undamaged store still opens. |
| R11 — rollback coverage for derived indexes | **Closed `e6f5de83`.** `session/persistence_tests.rs` stages each indexed mutation kind under an injected store failure and compares the whole `SessionState`, so records, private indexes, sequence and receipt maps must all come back | Removing one inverse at a time from `session/journal.rs` makes `failed_persistence_restores_every_derived_index` fail for the entry, queued, turn and reverse-dependency indexes (observed individually); no-op'ing all four fails that test and the rejected-plan test. `a_rejected_plan_leaves_indexed_reads_unchanged` rejects a plan after it staged a conversation, an entry and successors, then asserts paging, queued selection, turn membership and ready dependents are unchanged. |
| R12 — observation wait without a lost wakeup | The client wait companion must subscribe before it decides there is nothing to deliver | `TaskDriver::changed` subscribes only when polled (`crates/ion-core/src/session/scheduler.rs:258-267`), so reading `observations_after(cursor)` and then awaiting it loses a commit that lands in between (review finding 8, reproduced). Replace with a cursor-aware wait that subscribes before checking coverage, or a subscription created before the read, keeping notifications coalescible and observations authoritative. Regression: deterministic barriers place commits before subscription, between read and wait, and while asleep; each yields a delta or a required resnapshot, and close resolves with no new commit, without relying on yields or sleeps. |

R1–R5 precede further runtime feature expansion. R6's bounded-work gate precedes expanding worker fan-out. R7 may use small provider fixtures — including **one bounded live provider slice** — to inform contracts without waiting for broad storage benchmarking, and it should: instruction/control/wire fidelity can change the durable request representation, so learning that after optimizing frozen-request storage would be backwards. The transaction overlay is implemented. The current order is (1) R12, (2) resolve the provider design's §10 review gates, then one live provider path plus two materially different wire fixtures, (3) bounded read/edit/exec in a disposable workspace with limits from the first mutating live run, (4) the rest of R6 residency and the remaining bounds alongside R8, (5) a usable single-agent terminal surface with evaluation results, and only then (6) joined workers against a concrete workload hypothesis. R7/R8 deliver the single-agent baseline before resuming joined-worker/control-surface expansion, and daily single-agent usefulness precedes group control. Start small evaluations with M2; M7 is broader optimization, not the first effectiveness check.

Until that work, one constraint is explicit: a foreground slot serializes turn *admission*, not transcript writes, and a plan may target an existing conversation, so background work must not freely inject model-visible exchanges into a busy conversation. Prefer attributed input delivered at a safe boundary over unrestricted background projection writes; a regression that pins the refusal is needed before workers are expanded.

After that baseline, resume K6 with explicit joined-result selection and aggregate-cycle rejection, then reuse, nested limits and group control. A closure receipt must never be treated as a successful answer merely because the last member settled. Keep broader provider catalogs, equivalent inline/fullscreen UIs, MCP/extensions and advanced delegation in their later passes. Minimum sandbox/permission boundaries and run/output limits are not deferred to those broader passes.

Follow [docs/core-runtime-migration.md](docs/core-runtime-migration.md) and [docs/source-layout.md](docs/source-layout.md). The five pre-rewrite gates are closed; their evidence and decisions are recorded in [docs/r0-kernel-gates-2026-09-12.md](docs/r0-kernel-gates-2026-09-12.md).

Accepted R0 decisions:

1. one typed async `TaskKind` contract with durable complete checkpoints, invocation-generation fencing, durable cancellation mark + local signal, and fresh abort invocation;
2. append-only immutable entries with derived head/edit context and stable fork cutoffs;
3. no generic durable `Effect` entity in the fresh core;
4. one session-local monotonic sequence privately mints typed local IDs and commit cursors;
5. a small independent provider-neutral `ion-ai` crate owns model request/stream/result/error types and a scripted service.

The rewrite is now past the pre-rewrite/prototype stage. Continue from the production kernel; add another isolated prototype only when a concrete production design question is cheaper to answer that way.

## 2. Clean production core

Do not maintain old/new production runtimes in parallel. Git history preserves the old implementation.

### K0 — `ion-ai` contract crate

**Status: implemented and validated.**

The accepted R0.5 provider-neutral model contract plus scripted service lives in the independent `ion-ai` crate. No production HTTP/auth/catalog implementation belongs here yet.

### K1 — storage-independent domain

**Status: implemented and validated.**

The fresh core contains only the target durable nouns:

```text
Session
Conversation
Entry
Input
Task
TaskOutput
Artifact
```

Identifiers use distinct Rust wrappers over one session-local ordered namespace; `SessionId` is global.

No durable Agent row, lane, Operation, or generic Effect remains in the production core.

A `Conversation` may have a history parent/cutoff and/or owning task. Those are different edges.

### K2 — session writer + deterministic in-memory store

**Status: implemented and validated.**

The current kernel has one serialized semantic mutation line, typed atomic mutation batches and a deterministic in-memory store. Implemented capabilities include:

- create session/root conversation;
- create independent/forked/owned conversations with reciprocal task ownership;
- append immutable entries/context controls, rejecting a head or edit whose resulting projection is not a complete provider-safe context (orphaned tool result or dangling call) without consuming sequence values;
- accept/dedupe/place inputs with exact request-key replay/conflict;
- create tasks and dependency edges;
- one authoritative foreground-turn slot per conversation with turn-scoped group cancellation that leaves terminal, background and owned work untouched;
- reserve execute/recover/abort invocations with generation fencing;
- replace durable task checkpoints/output;
- durably mark cancellation and fence stale/normal writes;
- apply terminal/abort closure atomically with successor writes;
- explicit input disposition transitions;
- produce bounded committed observations with resnapshot-on-overflow semantics;
- expose a bounded `SessionSummary` (counts and cursors, no payloads) and a paginated fork-visible transcript read with an exclusive cursor;
- reject failed transactions without publishing IDs, commits or partial successor state.

K2's lifecycle paths are exercised through the K3 production driver so they are not test-only dead code.

### K3 — task driver

**Status: core driver implemented; reviewed turn-control repair closed at `b4730bc8`.**

Implemented now:

- explicit `drive_task`; inspection/snapshot does not start work;
- task-kind registry keyed by kind + schema revision;
- execute for pending tasks, recover for already-running durable tasks and abort for cancelled work;
- task futures run outside the session mutation lock;
- invocation-scoped `TaskContext` checkpoint commits re-enter the session writer briefly;
- durable cancellation mark + process-local cancellation signal + fresh abort generation after the old invocation joins;
- local duplicate-drive rejection for one task;
- dependency readiness enforcement at reservation;
- a never-dispatched task with a missing kind settles `Unsupported` without deleting the record; a running task whose implementation is unavailable blocks recovery instead of terminalizing;
- a known application failure settles durable `Failed`; handler errors and panics interrupt and leave the task running and recoverable;
- cancellation/settlement ordering is serialized under the writer: settlement wins if it commits first, otherwise abort owns cleanup.

R1–R5 and R9–R11 are closed; R12 remains the last client-wait gap before the provider surface freeze. Their acceptance gates precede the provider surface freeze; see §1.

The typed authoring adapter is implemented in `task/typed.rs`: `TaskRegistry::register_typed` erases a `TypedHandler` into the ordinary registry entry shape, input/checkpoint decode and result encode go through the durable JSON shapes, and terminal/failed/aborted/indeterminate stay distinct. An undecodable checkpoint or un-encodable result interrupts an already-dispatched task instead of terminalizing it; only never-dispatched work settles a structured `Failed`.

The storage-independent wait/capacity/close slice implements client task/dependency waits over committed-state notifications, independent optional model/tool/process limits, driver-owned invocation lifetime across caller disappearance, and graceful/fault close with canonical-write fencing and local joins. Immutable dependencies reject self/forward references by requiring existing tasks; there is no dynamic invocation wait graph. Deterministic tests live in `k3_waits.rs`, `k3_close.rs` and the capacity unit tests. Format, strict workspace Clippy and full workspace tests pass for this slice. Initial cancellation dispatch, cancellation during saturated capacity admission, and interruption/uncertainty handling are covered by outcome-sensitive tests in `k3_abort.rs` and `k3_driver.rs`: exactly one Abort invocation, no normal execution, separate bounded cleanup admission, interrupted invocations that stay running and recoverable, known-failure versus indeterminate settlements, and blocked recovery for a running task whose implementation is unavailable. Persisted reopen coverage remains open.

K4 now supplies persisted reopen, exclusive-ownership and explicit-drive evidence. Cross-process dispatch-window crash recovery is witnessed at `8c91cb28`: a child process dies inside a tool call after the dispatch record is durable and after the external effect landed, and the parent recovers it as Indeterminate without repeating the effect.

### K4 — fresh SQLite session store

**Status: SQLite schema v5 with exclusive ownership implemented; restart coverage is closed (R5, `fa991317`) and bounded storage repair remains open (R6).**

The session owns resident semantic state; a private persistence sink accepts validated batches before prepared state is installed and observations publish. Persistence errors fence the session and fault-stop live async invocations. Fault tests cover atomic rejection of terminal/successor writes, unchanged resident state/observations, and live invocation shutdown. This is in-memory fault evidence, not crash durability evidence.

Bounded residency and the persistence interface are accepted targets, but bounded residency remains unimplemented (R6). Resident records are copy-on-write behind `Arc`, so a commit does not deep-copy unrelated durable records and a checkpoint touches only its own record; transcript projection still materializes visible history. The per-commit map-structure clone was removed at `54367ada`: preparation is now in-place and journaled, with rollback before fencing on persistence failure. The decision and its invariants are recorded in `docs/core-runtime-migration.md`. The durable write set in `MutationBatch` is what a backend persists and can reconstruct from, covered by `committed_write_set_reconstructs_resident_state`. Restricted task finalization, foreground-turn membership and the typed task authoring adapter are implemented. Planned owned-conversation/scratch finalization is K6 work and does not block basic SQLite.

The fresh per-session schema is implemented at version 5. Do not migrate old lane/operation tables into the new core.

Development-era old databases may be archived/refused under the pre-1.0 policy. A later migration is written only if preserving old sessions is actually worth the complexity.

K4 should preserve the K2 ordering contract: validate/build against resident state, durably commit the complete mutation batch, update resident indexes/state, then publish committed observations. SQLite I/O must not become an alternate semantic writer.

### K5 — one generation/tool chain

**Status: implemented; provider catalogue and mid-turn steering remain.**

The built-in kinds live in `crates/ion-core/src/builtin/` and use the ordinary task contract:

```text
input
 -> generation
 -> tool A + tool B
 -> post-tools/join
 -> final generation
```

`Builtins` registers `generation`, `tool` and `post_tools` over one `ion_ai::ModelService` and one `ToolCatalog`; the scheduler branches on no built-in name. Generation reads its conversation transcript in bounded pages, projects it with `conversation::context::project` (which already carries any accepted input the session placed there), and records the complete model request, cutoff, the ids of the placed inputs that projection actually included, and the attempt count in its checkpoint before dispatch, so recovery replays the recorded request instead of silently building a different one. It settles with a plan that appends its transcript entries and creates the tool children plus the join; it places and consumes no input. Tool B may settle before A; each tool owns its own result entry, and projection restores call order. A stream that ends without a completed response settles `Failed` with the retained evidence instead of appending a partial answer, and an in-flight model or tool call observes the durable cancellation signal.

The tool kind records a durable dispatch before handing a call over, so recovery distinguishes never-dispatched work from a dispatched call whose outcome is unknown. A non-retry-safe call in the latter state settles `Indeterminate` with a recorded result stating the uncertainty — it is not repeated and not reported as stopped — and `Tool::retry_safe` is the explicit opt-in for repeating. `TaskDriver::cancel_turn` marks the turn's members, signals live invocations, and drives abort cleanup for members that never dispatched, so a cancelled chain closes its exchange and releases the foreground slot without a manual drive.

Input admission applies the `DESIGN.md` §11 mode/state policy in one commit and places the input's entry when it starts or binds the turn that answers it: `Session::admit_input` starts the turn that answers an input when its mode and the conversation state call for one and otherwise queues it, while a refused admission (submit on a busy conversation) admits nothing. `TaskDriver::admit_input` uses the driver's configured `TurnTemplate` and `TaskDriver::submit_input` takes an explicit turn request, so the scheduler branches on no built-in name. The generation places and consumes no input: the session writer placed the entry with the turn, and the binding records which attempt answers it. Exact request-key replay is unchanged and opens no second turn; a replay reports the input as queued when that is what the original admission did.

Queued input is drained by the settlement that releases a conversation's turn slot, scoped to the conversations whose slot that settlement actually released, one input per successor turn and in admission order; `TaskDriver::schedule_next_turn` exposes the same step explicitly, which is how a conversation reopened with durable queued input resumes. Without a template nothing is scheduled automatically and a turn-starting mode on an idle conversation is refused rather than dropped; a turn whose kind the driver cannot run is never bound at all, so the input stays answerable.

Still open for this slice: a production provider catalogue; mid-turn steering (a steer on a busy conversation is queued and delivered at the next turn boundary, but injecting it into a running generation is unbuilt) and a per-conversation paused/idle policy; context/output bounds (P2); a per-tool reconcile/adopt seam for tools that must query an external identity before retrying; and a bounded rule for cleanup created by cleanup (a successor born cancelled during abort cleanup still needs an explicit drive).

### K6 — workers as owned conversations

**Status: retained workers and turn receipts implemented; further expansion paused behind the repair gate and single-agent baseline.**

A `TaskPlan` can now create conversations it owns: `TaskPlan::create_conversation` returns a plan-local handle that planned entries and successors may target (`PlannedTarget::{Existing, Planned}`), the conversation is created before them, and it is owned by the settling task with the reciprocal edge in the same commit as the outcome. Context seed is explicit — `PlannedConversation::fresh`, or `inherited` at a stable cutoff validated exactly like any fork — and the plan bounds now cover conversations. `tests/k6_workers.rs` covers fresh and inherited workers, ownership reciprocity, seeding a worker with a brief and a retained task, reopen, a foreign plan handle, an invisible cutoff and the enforced bound.

`builtin::worker` is the production retained spawn: a `worker` task takes a `WorkerSpec` (brief plus optional inherited seed) and its settlement plans the owned conversation, the brief as a projected transcript entry, and the worker's initial generation in its own turn. `TaskDriver::{conversation, owned_conversations}` give bounded discovery without materializing the session.

Retirement is implemented as a read-only archive: a durable `Conversation.retired` flag (schema version 2) set only for an owned conversation with no foreground turn and no non-terminal task, rejected through every writer path (including settlement plans) while history, ownership, terminal outcomes and checkpoints stay readable, with reactivation clearing the flag and starting nothing. Queued-but-unstarted input is cancelled in the same commit.

Worker-local turn scope is implemented: `PlannedTurn::{Inherit, Own, Background}` replaced the `background` flag, and `Own` opens the target conversation's foreground slot in the same commit that creates the successor rooting it (rejected with a full rollback when the slot is already held). The spawned worker's initial task uses `Own`, so a worker's run occupies its own conversation's turn: follow-ups queue behind it and drain into their own successor turns, `cancel_turn` on the worker's root stops exactly that run, and the creator's conversation is idle as soon as the spawn settles.

Turn completion is durable: the turn root records the member whose settlement closed the turn, written with that settlement and the slot release, and `TaskDriver::{turn_closed_by, wait_turn}` expose whole-chain closure, not answer selection or success.

Deferred until the repair gate and single-agent baseline above: **the dependency edge on a turn plus an explicit result-selecting collector** (a task cannot yet depend on a turn, and the edge must reject the aggregate cycle a backward-only reference check cannot see), the command surface for send/follow-up, inspect and wait, interruption scoped to one worker run as a first-class operation, reuse, and nested ownership limits.

Implement the control surface through the same session/task kernel:

- fresh-context worker;
- inherited-context worker at safe cutoff;
- foreground/joined run;
- retained/background spawn;
- follow-up/reuse;
- send/inspect/wait/interrupt/retire;
- nested ownership limits;
- waits that do not monopolize execution capacity;
- subtree cancellation barriers.

There is no separate agent registry.

**Exit for K0–K6:** a new production core replaces the old lane/operation/agent/effect machinery; old core files and temporary R0/P1 prototype implementations are removed rather than left as permanent compatibility paths.

## 3. P1 — durable execution correctness

The fresh production core closes P1 when deterministic tests cover:

1. durable input acceptance and exact request-key replay/conflict;
2. two tool tasks settling out of order with call-order model projection;
3. worker conversation surviving creator task settlement;
4. capacity-safe waits;
5. cancel/settle both orders and late callback fencing;
6. process loss across retry-safe, reconcile/adopt and indeterminate external actions;
7. atomic terminal settlement + successor/ownership writes;
8. reopen without automatic execution before explicit drive;
9. caller disappearance before/after admission;
10. second writable owner rejection including abnormal predecessor exit;
11. missing task-kind behavior without data loss;
12. dependency/self-wait cycle rejection;
13. panic/failure/host-close join policy;
14. explicit durable input disposition;
15. worker creation/message/cancellation races under one session writer.

Fresh-core coverage now includes exact request-key replay/conflict, atomic terminal successor rollback/commit, explicit input disposition, generation/cancellation fencing, missing task kind, panic/error settlement, no-work-on-inspection and both high-level cancel/settle orderings. The remaining items are not implied complete by the historical prototypes.

Historical isolated P1/R0 prototypes remain regression evidence only until the fresh core covers these invariants.

## 4. P2 — storage, output and history

Semantic rule: one top-level session has one crash-atomic canonical store boundary. Root and worker conversations share it. Independent sessions do not.

Leading physical candidate:

```text
Ion data root/
  sessions/<SessionId>/
    session.sqlite
    artifacts/

  catalog.sqlite   # optional rebuildable discovery cache only if useful
```

Compare the removed root-wide SQLite layout (historical, Git-history-only) against per-session stores under many active sessions, large/cold histories, WAL/checkpoint pressure, backup/archive/delete, corruption isolation and restart/repair.

SQLite remains the baseline. Benchmark Turso only if representative measurements show engine-level overhead or a concrete sync requirement makes it relevant. Do not add concurrent canonical writers merely because an engine supports them.

Output tests cover large model/tool/process streams, spill thresholds, slow/dying storage, disk exhaustion, process loss, artifact publication/orphan cleanup and watch overflow.

History tests cover long transcripts, dense heads/edits, deep forks and at least 100k terminal tasks with a small live set. Opening/resuming must not decode all historical task payloads.

**Exit:** justified physical topology, measured query/RSS/WAL/artifact behavior, supported backup/repair path, explicit provisional-output loss bounds and bounded active residency.

## 5. Component pass A — execution environment and tools

After the kernel contract is stable, redesign rather than automatically preserve current tool/process modules.

Target boundary owns:

- workspace identity/binding/isolation;
- files/read/write/edit/search;
- shell/process lifecycle;
- background jobs;
- sandbox/policy enforcement;
- narrow tool execution API;
- MCP adaptation later.

Ordinary tool definitions receive no arbitrary session mutation authority. Built-in trusted task adapters mediate durable worker/job creation and other session-affecting operations.

Port path-safety, process-cleanup, output-bounding and policy algorithms only after they pass the new boundary.

## 6. Component pass B — AI/model/provider/auth subsystem

Extend the accepted `ion-ai` boundary using the useful separation seen in `pi-ai` without copying its TypeScript API:

```text
ModelService / model registry
  Provider
    auth + model catalog + endpoint policy
      API adapter
        provider wire protocol
```

Provider-neutral messages/tools/stream results sit above reusable API adapters. Several providers may share one OpenAI-compatible/Responses/Anthropic/etc. wire implementation.

Provider subsystem owns model discovery/refresh, capabilities/pricing metadata, auth resolution and wire parsing. The application/host owns credential persistence. Dynamic model-catalog cache is independent of session storage.

Ion-specific requirements:

- typed provider error categories;
- provider API contains no session/task IDs;
- durable retry/backoff/usage belongs to the generation task;
- provider/SDK hidden retries are disabled or controlled;
- opaque provider replay metadata may survive on provider-neutral assistant content for same-provider continuity;
- deterministic faux/scripted provider for tests.

Only after the fresh core can run scripted generation should OpenRouter/OpenAI-Codex/current auth code be selectively ported.

## 7. Component pass C — TUI/client architecture

The TUI is a client of runtime truth, not another state owner.

Validate conversation view, session/group summary, worker focus, target-safe per-conversation drafts, approvals, narrow terminals, Unicode/multiline input, resize/output floods, reconnect/resnapshot and terminal restoration.

Review the existing terminal/editor/rendering primitives individually. Do not preserve the current large TUI application structure merely because some widgets are reusable.

## 8. Component pass D — extensions/MCP/authority

Add extensibility only after the core tool/task/client boundaries exist.

Test tool contribution, observation hooks and any justified typed behavior contribution; explicit replace/remove; stale approval/revocation; missing implementation on recovery; cancellation/oversized output; trusted local subprocess versus actual OS sandbox isolation.

MCP is an execution/tool adapter, not a parallel runtime or storage authority.

## 9. Component pass E — external protocols and application shell

Rebuild ACP/JSON/RPC/print/CLI/session-management adapters on the one command/observation contract. Then review settings, export/import, updater/package behavior.

These adapters may wait/reshape responses for protocol compatibility but must not introduce alternate session semantics.

## 10. Delivery milestones

| Milestone | End-to-end result |
|---|---|
| M1 — repaired core | existing fresh kernel plus R1–R5 correctness repairs and targeted crash/cancel evidence; broader K6 controls do not block M2 |
| M2 — real coding loop | R6 bounded-work gate and R7/R8: provider, instructions/configuration, read/edit/exec, minimum permissions, streaming headless client, compaction, limits and a small coding regression suite |
| M3 — controlled workers (after daily single-agent usefulness) | retained/nested workers, messages/results, workspace policies, background jobs, group limits and TUI group control |
| M4 — daily coding | provider/auth/model catalog breadth, images, skills/resources, search/completion, editor/settings/export and hardened TUI |
| M5 — parallel implementation | worktree-backed mutating workers, retained patches/evidence, apply/reverify/conflict/cleanup |
| M6 — extensibility/interoperability | scoped extensions/MCP plus stable useful Rust/JSON/ACP client surfaces |
| M7 — effectiveness optimization | broaden the evaluations begun at M2; compare context, editing, compaction and delegation against the baseline; higher-level systems only if they improve measured outcomes |

No milestone is “Pi parity.” Pico supplies minimal-harness design evidence; Codex supplies production-engineering evidence; Ion chooses its own tested contracts.

## 11. Evaluation rules

Use fake model/environment implementations, controllable clocks and storage barriers for deterministic races. Run both orderings of every important race. Crash-inject before/after admission, external dispatch, checkpoint, result, terminal settlement, successor creation and artifact publication.

Keep runtime performance separate from model effectiveness. Record source revision, exact workload/configuration, memory, storage bytes, query counts, lock/checkpoint behavior, restart time and externally verified task outcomes.

Agent-effectiveness optimization starts from a stable M1/M2 baseline. More context, more agents, memory systems and richer coordination surfaces must demonstrate benefit rather than receive architectural preference.

## Evidence log

2026-09-14: R11 closed at `e6f5de83`, covering the rollback gap the Rust-surface review reproduced by deleting the inverses in an isolated copy. Sensitivity was re-established here rather than assumed: each of the four derived-index inverses was no-op'd in turn and `failed_persistence_restores_every_derived_index` failed for every one (entry index, queued inputs, turn membership, reverse dependencies), and no-op'ing all four also failed the rejected-plan case, which previously passed for the wrong reason — the settling task had never been reserved, so the command was refused before it staged anything. The rejected-plan regression now reserves the turn first, rejects after a conversation, an entry and successors were staged, and checks the reads those indexes answer. Not established: allocation-failure rollback (a panic mid-command still relies on the explicit rollback path), and whether the journal should roll back on drop, which the review raised as defensive encoding rather than a reproduced defect. Format, strict all-feature workspace Clippy and the full workspace suite (200 tests) pass.

2026-09-14: R10 closed at `6618e1b1`. Two latent capabilities were removed rather than checked at runtime. `TaskPlan` and `TaskCompletion` no longer derive `Clone`, which makes the review's diverged-clone witness (a handle from one branch binding an object in another) unrepresentable instead of relying on the writer's plan-id/index check; no in-tree consumer needed the clone, and an external compile-fail test would require a new dev-dependency for a property the type system now enforces. The three insertion owners use occupied-entry rejection before writing, so a rejected duplicate leaves records and private indexes untouched; the new state-comparison test fails against the previous owners (observed: the rejected entry, task and input were each replaced). Reconstruction now validates the loaded state — sequence and commit bounds, references, ownership reciprocity, dependency ordering, turn membership and receipts, lifecycle combinations and retirement quiescence — and refuses the session before a writable owner exists, with eight stored fixtures in `k4_sqlite.rs`. Format, strict all-feature workspace Clippy and the full workspace suite (198 tests) pass. Not established: byte-level allocation bounds and a repair path for a refused store, which remains deliberate.

2026-09-14: R9 closed at `682307e2` (opening repair `1d370462`). Generation now owns prepare/open/collect under one cancellation select with cancellation polled first, and its abort reads the durable checkpoint: a prepared attempt is reported as such instead of the abort claiming the attempt never ran, and unreadable attempt evidence settles Indeterminate rather than being read as never dispatched. `crates/ion-core/tests/k5_provider_cancellation.rs` covers a dropped opening future, a dropped stream, a durable mark before any dispatch (no provider open), a settled answer surviving a later cancellation, and a damaged checkpoint reaching abort over a real close/reopen. Watched failing against the pre-repair abort handler: 4 of the 5 cases fail, the settled-answer ordering case passes both ways. Format, strict all-feature workspace Clippy and the full workspace suite (195 tests, 31 suites) pass. Not established: server-side cancellation or zero billing after a dropped request, a durable per-attempt usage/error ledger, and deadline ownership, which R7 owns at this boundary.

2026-09-14: engineering audit at `1d370462`, documentation only. Retain the kernel nouns, one writer, journal/index owners, boxed owned futures and separate provider crate. Rechecked generation cancellation, transaction rollback/publication, plan cloning, duplicate insertion and observation subscription paths against current source; earlier reproduced R10–R12 findings remain open, not newly reproduced here. R9's partial fix does not close its remaining race matrix. Corrected stale K3/K4 and migration/layout claims, the malformed R9–R12 table, and the execution order. Provider proposal remains unapproved for trait freeze: §10 now requires one turn-budget owner, typed recoverably-blocked revision/profile handling, one retry-policy authority and shared neutral stream validation. R7 must also close typed authoring/read parity from the Rust-surface review before freezing that surface. No runtime, live-provider, terminal or performance checks were run for this audit; `1d370462` supplies the preceding Rust evidence, not proof of these open contracts. The legacy application is excluded; `ion-terminal` is an active workspace member (contrary to the temporary handoff).

2026-09-14: R9 opening-cancellation repair: `k5_provider_cancellation.rs` first reproduced the opening-future leak (drop-witness timeout), while its collection counterpart passed. Generation now selects cancellation against one owned open-and-collect future, prioritizing an already-ready cancellation signal before polling the provider. Both tests pass and require the provider owner to drop, the drive to join and settle through generation 2, retained attempt checkpoint evidence, and no assistant entry or tool child. Format, strict all-feature workspace Clippy and full workspace tests pass. R9 remains open pending deterministic coverage immediately before opening and both cancellation/settlement writer orders; deadline ownership remains R7 work. This change preserves the existing cancellation outcome and attempt checkpoint; it does not establish remote cancellation, known usage or a durable per-attempt ledger.

2026-09-14: Rust and crate-surface review of `87225783` (rechecked through `01cff5cd`), recorded at `docs/research/rust-surface-review-2026-09-14.md`, with no code changed. Verdict: keep the kernel architecture, the boxed-future boundary, the in-place journal and the derived indexes; do not freeze the current public authoring or provider surface. Two High findings reproduced: a provider service that blocks inside `stream` defeats cancellation because generation awaits the opening future outside its select, and `TaskPlan: Clone` lets a handle from one clone bind a dependency in another (now R9 and R10). Two Medium findings reproduced: duplicate insertion replaces the record it rejects and then has no inverse, with reconstruction accepting metadata that disagrees with the records, and disabling every derived-index inverse in the journal leaves all scoped suites green (R10 and R11). Further Medium findings, not yet placed as slices: typed authoring cannot use the ordinary invocation reads or late registration; typed provider errors are formatted into strings before policy can consume them, so the §13 "failures cross as facts on the invocation" claim is stronger than the drive result; the request basis is captured after paging rather than atomically before it and the task read API has no upper cutoff; "bounded" currently means counts rather than bytes, with `limit + 1` panicking at `usize::MAX`; and residency, ancestor-id materialization for fork pages, and payload cloning remain on hot paths. The review's own three-run measurements (4,000 and 16,000 turns) confirm flat admission and settlement medians (~80 µs, ~105 µs) with reopen still history-proportional, and it explicitly declines to claim asymptotic bounds from them. Its retention list (independent neutral crate, one writer, typed ids, owned invocation lifecycle, honest recovery uncertainty, unknown-usage distinction, real crash evidence) is recorded as "do not churn".

2026-09-13: holistic design/implementation/roadmap review of `5b2be827` (Astra), no code written. Verdict: a credible durable kernel, not yet evidence of a competitive coding agent — over-built in sequence, not over-abstracted, with recovery and worker bookkeeping ahead of provider fidelity, execution safety, context management and interactive usefulness. Ranked gaps: real request assembly and provider behavior; safe effective read/edit/exec; context and run control; streaming/intervention; bounded storage; delegation. Accepted consequences, applied above: the R6 overlay still lands first, but a **bounded live provider slice** now precedes completing R6 residency, because instruction/control/wire fidelity can change the durable request representation; read/edit/exec and limits come next; daily single-agent usefulness precedes group control; and joined workers wait for a workload hypothesis. Corrections applied to the documents rather than deferred: the tool-recovery claim is now policy-level (recorded evidence does not identify the implementation, so a same-named replacement is indistinguishable); "accounting for each attempt" is now stated as a counter plus invocation facts rather than a durable attempt ledger; §14 no longer implies the session writer serializes workspace execution, which runs outside mutation authority; the roadmap's schema-version and K3/K4 status lines match the code again; `docs/source-layout.md` says the legacy application crate is excluded from the workspace rather than deleted. Newly recorded as open work rather than prose: a fork's page cost grows with its ancestor's entire visible id history; `summary`/`snapshot` are unbounded surfaces that ordinary clients must stop depending on; generation captures its cutoff after paging, so a read boundary is not yet finite before reconstruction; and background plans must not inject model-visible exchanges into a busy conversation. Retained from the same review as sound and worth refusing to expand: task/plan/turn machinery (no durable Turn entity), the mutation/observation split, and the generation/tool/post-tools split. Flagged for the user, not actioned: deleting the ~27k-line excluded legacy application crate once its provider/TUI leaf material has been mined.

2026-09-13: R6 third and fourth slices, measured and re-measured (`54367ada` overlay, `22a7399b` indexes). `Transaction::new` cloned all nine resident maps per command, so every commit cost the whole history before anything was persisted and every index added to a scan path paid that toll. A command now prepares its writes directly on resident state through `session/journal.rs::Editor`, which records the previous value of each write and replays them in reverse when the command is rejected; on success the prepared writes are the committed state, and if persistence fails they are taken back before the session is fenced, so resident state never keeps a command that is not durable. Measured at 16000 turns: 33.98s -> 4.72s for the overlay alone and 3.6s with the indexes re-landed (about 800 -> 4200 turns/sec); admission p50 584us -> 85us and settlement p50 1081us -> 111us, both now flat as the resident set grows (80us and 125us at 4000), and a plain append is about 100us at 256 entries and at 8000 entries. Re-landing the task-side indexes (tasks by turn, reverse dependency edges, queued inputs) was measured before and after the overlay on the same workload: 3371 -> 4230 turns/sec and settlement 174us -> 111us, where the same change had previously made the build slower (800 -> 471 turns/sec). That pair of measurements is the point: the indexes were never the problem, the per-commit copy was. `k2_session.rs::a_rejected_admission_leaves_resident_state_unchanged` pins the new boundary by comparing the whole snapshot, so a staged input, a consumed id, a request key and every derived index must all be restored. Not established: whether installation-by-mutation is as cheap as claimed under a *failing* store (the rollback path is tested, its cost is not), and the fork-page and residency costs recorded above remain.

2026-09-13: R6 ordering evidence, measured. Before touching storage again, an attempt to index the task-side scans (tasks by turn, reverse dependency edges, queued inputs) was written and then withdrawn: it made commits **slower** (16000-turn build 800 -> 471 turns/sec, settlement p50 743us -> 1081us, admission 584us) because every added index is another whole map for `Transaction::new` to clone per commit. That is the ordering result the measurement was for: the per-commit clone is not a constant-factor problem to be traded against scans, it is the term that makes every other index pay a toll, so the overlay must land before any further indexing. The withdrawn patch and its numbers are kept in the session record and the finding is recorded here rather than as a commit. Indexing the settlement scans is expected to become a win once a commit costs its dirty records instead of the resident map set; that must be re-measured rather than assumed.

2026-09-13: R6 second slice, measured (`642d8159` adds the read measurement, `ec1b05cb` the repair). `Session::conversation_entries` materialized the whole fork-visible transcript on every page and then sliced it, while generation asks for every page, so a complete read was quadratic in transcript length: a harness read of the same transcript in pages of 64 took 46.36ms at 4000 entries and 923.7ms at 16000, with per-page cost rising from 736us to 3695us as the transcript grew. `SessionState::visible_page` now answers from the per-conversation entry index: one ordered range read for a conversation with no history parent, and ancestry plus page for a fork, whose inherited prefix is materialized as ids (eight bytes each) rather than cloned records. After: 1.08ms and 4.24ms, per-page 17.1us and 16.9us, so a complete read scales with the transcript. Cursor semantics are unchanged (an invisible cursor is still refused, a zero limit still has no continuation). Not established: a fork's page cost still grows with ancestry depth, and nothing bounds the size of one entry, so a page of large entries is bounded in count but not in bytes. Remaining R6 work is unchanged: the per-commit whole-map clone, settlement-time scans over all tasks, full-history hydration at open, frozen-request payload duplication, and every byte cap.

2026-09-13: R6 first slice, measured (`904c9633`, `478af990` add the harness and its attribution; the repair and its regression are in the commit that follows). The harness (`crates/ion-core/examples/storage_measure.rs`) drove the real writer, store and open path and showed per-commit cost growing with history, not with active work: a single plain `append_entry` went from 213us to 1519us within one 8k-append run, admission of one turn was 900us with settlement at 340us, and an 8k-turn build ran at 696 turns/sec with a 100k-turn run failing to finish in 11 minutes (RSS 488 MB). Attribution used the same total work spread over one conversation versus 64: with 8000 entries resident, one conversation cost 1519us per append while 64 conversations cost 141us, so the dominant term was per-conversation history materialization rather than whole-resident cloning. The cause was `SessionState::visible_entries`: it scanned **every entry in the session**, cloned each match, and built a `HashSet`, and `Transaction::append_entry` called it unconditionally even when the request named no head or edit. The repair adds a derived `entries_by_conversation` index written by the single owner of entry records (`SessionState::insert_entry`, used by both `apply_mutation` and reconstruction, so loading a retired conversation's history does not go through the acceptance check for new work) and reads the transcript through it; a plain append now validates without materializing anything, and an append that names a head or edit materializes the transcript once instead of twice. Measured after: append p50 58us/129us at 2k/8k entries (11.8x at 8k, growth now shallow), admission p50 197us, 8000-turn throughput 1650 turns/sec (2.4x). Still unbounded and still open: the per-commit whole-map clone (`Transaction::new`), the settlement-time scans over all tasks (`cancel-by-turn` and slot release), full-history hydration at `Session::open`, frozen-request payload duplication, and every byte cap. `k4_sqlite.rs::the_entry_index_agrees_with_records_before_and_after_reopen` pins the index against the records it is rebuilt from, including interleaved conversations and a fork prefix.

2026-09-13: R5 restart-safe observations at `fa991317`, plus the R4 provenance repair at `aba6b92c`. R5's defect: `Session::open` installed resident state with an empty event buffer and no notion of what that buffer covered, so `observations_after(Some(cursor))` with a cursor behind the loaded commit returned `reset_required: false` and no events \u2014 a client was told it was current while the commits it had missed were unreachable. Reproduction is the roadmap's own case: commit, retain the cursor, commit again, close, reopen, ask from the old cursor; before the repair it returned an empty successful delta. The session now records the earliest commit it can serve a delta for, taken from its own commits while it lives and from the loaded commit on open, and a cursor before that boundary requires a resnapshot while one at the boundary resumes streaming, so a handover costs one resnapshot rather than a permanently blind client. The review of R4 at `aba6b92c` fixed three findings in the first implementation: selection-based applicability in retry (an `Omit` edit passed the check while removing the content), abandonment blocked by the same condition, and frozen-request provenance inferred from a cutoff rather than from the projection that built the request; provenance now comes from a contribution set the projection computes where edits apply, which also removes a cross-conversation false positive. Format, strict workspace Clippy and the full workspace suite (188 tests) pass. Not established: whether a restarted session should also bound how long `coverage_start` remains in force (it is never cleared, which is conservative), and R6 still owns the read bounds that make a retry or a reopen cheaper.

2026-09-13: R4 input placement and answer attempts at `d6fad545`. The defect was structural rather than incidental: the generation's settlement appended the user entry for each input it had bound and consumed the binding in one commit, so placement existed only where an answer succeeded. Reproduced against the pre-repair tree at `eb611e07` with a minimal case (submit, cancel before the first token, read the transcript): 0 user entries, and the terminal turn root could not be re-driven, so the accepted request was stranded outside history. Placement now belongs to the turn: `admit_input`'s turn-starting branch and `bind_turn_for_input` append the entry and record `Placed{entry, turn}` in the same commit that creates the turn, while a queued input stays unplaced so a running generation cannot freeze a later turn's message into its request. `Consumed` was removed rather than repurposed: placement already happened by then, and attempt-level provenance lives in the frozen request (input ids only, no text duplication). `retry_input` and `abandon_input` are new explicit commands over the same turn-creation and disposition mutations; retry refuses a stale `expected_turn`, an unclosed turn, a retired conversation, or a placed entry no edit still selects into the context. The obsolete primitives were deleted (`TaskPlan::{consume_input, inputs}`, `PlannedInput`, `PlannedEntryRef`, `MAX_PLAN_INPUTS`, `TaskContext::assigned_inputs`, `TaskDriver::{assign_input, consume_input}`), and the schema went 4 -> 5 because the serialized disposition meaning changed. The slice also closed a divergence the review surfaced: retirement cancelled queued input in resident state only, so a reopen resurrected it and contradicted "reactivation resurrects no input"; the cancellation is now staged with the flag, and the reopened snapshot is compared against the live one (without the staged mutations the comparison fails, observed as `Queued` vs `Cancelled`). Format, strict workspace Clippy and the full workspace suite (183 tests) pass. Not established: steering a busy conversation mid-turn (still deferred to a turn boundary), matching a retry to a *specific* historical prompt once several attempts exist, and any bound on the transcript scan a retry performs (R6 owns read bounds).

2026-09-13: R3 fail-closed recovery at `9c19a331`, process-death witness at `8c91cb28`. Both built-in adapters decoded their durable checkpoint with `.ok()`, which made an existing-but-unreadable checkpoint indistinguishable from no checkpoint at all. Three consequences were reproduced against `1c31ea49`: a damaged frozen model request was rebuilt from current history and dispatched (and, because the transport kept failing, the task never settled); a damaged dispatch record was read as "never dispatched" and a non-retry-safe call ran a second time, reporting Completed; and a removed tool was answered "tool is not registered" as a completed result before prior dispatch evidence was consulted, while cancellation of the same state reported "cancelled before dispatch". Checkpoint decoding now returns absent, readable or unreadable, and an unreadable checkpoint settles Indeterminate without dispatching. Tool resolution reads evidence before the catalogue, requires the recorded call identity to match, and repeats only when the policy recorded at hand-over and the current policy both allow it, so a removed, replaced or newly retry-safe tool cannot retroactively resolve an older uncertain call. `tests/k5_recovery.rs` damages stored checkpoints behind a real close and reopen: 5 of its 9 cases fail against the pre-repair sources and all pass after. The remaining four are evidence that the repair did not overreach: an intact checkpoint is still replayed verbatim, a checkpoint column that is not JSON refuses reconstruction instead of rebuilding a wrong task, and a real child process killed inside the dispatch window leaves exactly one external effect which recovery reports Indeterminate without repeating. Format, strict workspace Clippy and the full workspace suite (179 tests) pass, and the new suite was run three times consecutively without a failure. Not established: the store's refusal to reconstruct an unparseable JSON column is coarse (one bad column makes the session unopenable) and has no repair path; and recovery of a call whose *effect* is only observable through an external system with its own idempotency identity is still out of scope.

2026-09-13: R2 durable turn control at `b4730bc8`. Reproduced the defect the review predicted: settling a turn root and then cancelling the turn left the root unmarked (`cancel_turn` skips terminal tasks), so the abort cleanup of a member that was still live planned a successor that was born runnable \u2014 work escaping a stopped turn. Turn cancellation is now durable turn state: `Conversation.turn_cancelled`, staged with the member marks in one commit and cleared in the commit that releases the foreground slot; successor inheritance reads that barrier together with slot ownership, so a settled root no longer weakens it. Terminal task outcomes are never rewritten. SQLite schema 3 -> 4 adds the column and refuses older development databases. `tests/k3_turns.rs` reproduces the defect and pins that a released turn's barrier does not reach a later turn in the same conversation; `tests/k4_sqlite.rs` pins that the barrier survives a graceful close and reopen. Format, strict workspace Clippy and the full workspace test suite (170 tests) pass, and the three touched suites were run three times consecutively without a failure. Not established: the review's suggestion to constrain background tasks from appending model-visible exchanges into a busy conversation is still only a design note, and joined-run result selection remains R6-later work.

2026-09-13: R1 exclusive writable session ownership at `5bf3ea87`. A session now holds a kernel-held advisory lock on a file beside its database from before any SQLite open, schema check or reconstruction until close has fenced writes and joined local invocations; the commit-cursor compare-and-set remains as the fence against a writer that bypassed ownership. The refusal is now a distinct `SessionError::SessionInUse` rather than a generic persistence failure, reconstruction reads one transaction, opening a missing database leaves no lock file, and a database that is already in WAL no longer takes an exclusive lock just to restate its journal mode. Acquisition waits up to ~100ms for a lock that may be mid-handover: the kernel releases the lock at process exit or exec, and a process that has just forked shares its open file descriptions with the child until that child execs, so without the wait an unrelated fork in the same process makes a free lock look held. The wait was measured against a reproducer that forked ~3900 children while four threads acquired and released locks continuously: with one attempt the threads panicked within milliseconds, and with the bounded wait 4027 acquisitions were delayed with a worst case of 9ms and none failed. `tests/k4_ownership.rs` covers the in-process refusal, that a refused open observes and changes nothing, release after graceful and fault close, a child process refused while the parent lives that commits after the parent releases, and 150 reconstructions against a concurrent raw writer asserting no visible entry is newer than the committed cursor. `k4_sqlite.rs` keeps reopen coverage and now exercises the cursor fence with a foreign writer. Format, strict workspace Clippy and the full workspace test suite (167 tests) pass. Not established: a torn-read race was never observed, which is not proof that none is possible, and read-only inspection by a second process is unimplemented — today every open is a writable owner.

Entries below are chronological evidence at their named revisions, not a second current-status list. The 2026-09-13 review at `888d103c` superseded earlier general claims of a complete cancelled-turn admission barrier and observation-gap handling; R2 closed the first at `b4730bc8`, and R5 still covers restart coverage. Existing tests remain useful but do not cover those scenarios.

2026-09-13: source design review at `888d103c`, including an independent Astra pass, preserved the architecture and identified R1–R8 above. User accepted the repair-first roadmap and long-term continuity update. No reproductions, Rust gates, live-provider runs or performance/effectiveness measurements were run in this documentation pass. Revision 7 of DESIGN records accepted repair contracts; implementation is open. Future joined-worker expansion is deferred behind the repairs and measured single-agent baseline.

2026-09-12: isolated historical P1 prototype at `78148e84d6120d5670a784ec3ecb07684577db1d` validated idempotent reopen, out-of-order effect settlement/call-order projection, retained worker lifetime, capacity-safe wait, cancellation fencing and abrupt-process recovery. Its failure/race evidence is retained; its mandatory `step -> plan` authoring API is superseded.

2026-09-12: P2 structural prototype at `0c45553538e0244e27cc13ac0719f8afb7d73cbb` validated atomic rollback within a session DB, independent writer-lock domains for separate session files, and rebuildable discovery metadata. No performance claim is attached.

2026-09-12: architecture revisions 3–5 removed knowledge/task-board systems from core, converged workers onto owned conversations, made fresh versus inherited context independent from session/lifetime, moved toward immutable transcript-derived context controls, reopened task authoring around async execute/recover/abort + durable commits, and made generic Effect removal an explicit pre-rewrite gate.

2026-09-12: R0.1–R0.5 passed together at `81c344d713f13b73e19232b4ad36dfe40ba663b9` in CI run `34718467220`. Accepted: typed async task contract with durable checkpoints/generation fencing; immutable entry/head/edit context + safe fork cutoffs; task-level external recovery without generic Effect; one session-local sequence backing typed local IDs and commit cursors; independent provider-neutral `ion-ai` boundary. See `docs/r0-kernel-gates-2026-09-12.md`.

2026-09-12: legacy production `ion-core` runtime/store/operation/tool code and historical prototype harnesses were physically removed at cleanup commit `2d273f78`; Git history remains the archive. The clean core no longer carries lane/agent/operation/effect compatibility paths.

2026-09-12: clean K0-K2 plus the first K3 task driver are validated at code checkpoint `a33e0fb22073857cc724244b23f99e0c12249147` by CI run `34725190754`. Superseded in part on 2026-09-13: handler errors and panics now interrupt and stay recoverable rather than settling `Failed`, and a running task with an unavailable implementation blocks recovery rather than settling `Unsupported`. Fresh-core evidence includes exact request-key replay/conflict, immutable fork/context checks, atomic owned-conversation/task links, bounded observations, invocation generation fencing, checkpoints, cancellation + fresh abort generation, terminal successor rollback/commit, dependency readiness, missing-kind preservation, panic/error settlement, no-work-on-inspection, duplicate local-drive rejection and serialized cancellation/settlement ordering.

2026-09-13: K3 storage-independent work completed and K4 prepared, with all gates green per commit. Added: notification-driven task/dependency waits; independent model/tool/process capacity with separate bounded cleanup admission; graceful/fault close that fences canonical writes before joining; resident-state/persistence split with fail-stop on persistence error; copy-on-write resident records so a commit does not deep-copy unrelated payloads; interruption distinguished from terminal settlement (`Indeterminate` for unreconcilable external actions, blocked recovery for an unavailable running implementation); atomic `TaskPlan` finalization; one authoritative foreground-turn slot that spans the whole chain with turn-scoped cancellation and a cancelled-turn admission barrier; commit-time context-control validation; loss-aware `ion-ai`; bounded session summary and paginated transcript reads; and a typed task authoring adapter. `MutationBatch` now carries the durable write set and `committed_write_set_reconstructs_resident_state` proves a store can rebuild resident semantics from it. See `knowledge/projects/ion/clean-rewrite-review-2026-09-12.md` for the review provenance and remaining limitations; no persistence or crash guarantee is claimed.

2026-09-13: K4 per-session SQLite store implemented. `Session::create(path)`/`Session::open(path)` are live over a fresh schema (version 1) covering session metadata and cursors, conversations, entries, inputs with durable request-key/admission-commit mappings, and tasks with dependency and ownership child tables. A commit applies its whole write set plus a commit-cursor compare-and-set in one SQLite transaction, so a second live authority is fenced and left closed rather than interleaved; the durability floor is WAL with `synchronous = FULL`. `tests/k4_sqlite.rs` shows a representative write set reconstructing an identical snapshot across close and reopen, duplicate-input replay surviving reopen, stale-authority fencing, and an interrupted task reopening as running with nothing implicitly started. A subprocess test commits three entries and then dies on `SIGABRT` with no cleanup; the parent finds every acknowledged commit durable and recovery idempotent. That establishes process-death durability, not power-loss durability, which is a filesystem property this test cannot observe. Driver-side reads are now bounded (`summary`/`task`/`conversation_entries`/`observations_after`/`changed`), observation recovery reports a coverage gap or an unknown cursor as `reset_required`, and opening a session performs no work.

2026-09-13: K5 chain backbone implemented. An invocation now reads through `TaskContext`: its own conversation transcript as bounded pages at an optional cutoff, and its own fixed dependencies' resolved outcomes in dependency order. A settlement dispatches the work it made runnable — successors the plan created plus dependents of the settled task whose dependencies are now terminal — so admitting a task still never starts it, and a candidate with no registered kind stays pending for an explicit drive instead of being settled `unsupported`. `tests/k5_chain.rs` runs generation, two tool branches settling out of order, a join that reads both resolved outcomes in dependency order, and a continuation generation that reads the transcript the join appended; the whole chain occupies one foreground-turn slot until its final settlement and runs on readiness dispatch alone after the turn root is driven. The model calls are still a scripted in-test kind rather than `ion-ai`'s model service plus a real tool executor, and input admission does not yet bind a pending user input to a generation task.

2026-09-13: K5 generation/tool chain made real over `ion-ai`. `crates/ion-core/src/builtin/` now holds the production `generation`, `tool` and `post_tools` kinds, registered through the ordinary `TaskRegistry` with no scheduler branch on their names. A generation invocation reads its conversation transcript in bounded pages, records the last observed entry as the frozen context cutoff, projects it with the pure `conversation::context::project`, appends the input durably bound to its task, calls `ion_ai::ModelService`, and settles with a plan carrying the user and assistant entries, the input consumption, one tool task per call and the join. The tool kind runs exactly one call against a `ToolCatalog` and settles with the real `ToolResult`; the join reads its dependencies in dependency order, appends one tool-result entry with consecutive tool messages, and starts the continuation generation. A stream that ends without an explicit `Completed` settles `Failed` with its evidence and appends nothing, so a partial answer cannot become history; in-flight model and tool calls both observe the durable cancellation signal. `TaskPlan` gained plan-local entry handles and input consumptions, so `Assigned -> Consumed(entry)` commits atomically with the entry that carries the content, and `TaskContext::assigned_inputs` reads only the inputs bound to the invocation's own task. Submission (`Session::submit_input`, `TaskDriver::submit_input`) admits the input and opens the answering turn plus the `Queued -> Assigned(task)` binding in one commit; a rejected submission admits nothing and duplicate request keys replay without opening a second turn. `tests/k5_generation.rs` covers the full chain with request inspection, replay after consumption, incomplete-answer rejection, a cancelled in-flight tool call, and submission rollback. Format, strict workspace Clippy and the full workspace test suite pass.

2026-09-13: K5 recovery and cancellation semantics corrected after an independent design review. The tool kind now records a durable dispatch before handing a call over, so recovery distinguishes never-dispatched work from a dispatched call whose outcome is unknown: a non-retry-safe call in the latter state settles `Indeterminate` with a recorded result stating the uncertainty instead of repeating the external action or claiming it was stopped, and `Tool::retry_safe` is the explicit opt-in for repeating. Each tool task commits its own result entry with its outcome, which is what DESIGN's completion-order chronology requires, so the post-tools join became a pure barrier that starts the continuation generation only once every call has a recorded result; a stream that ends without a completed response still settles `Failed` and appends nothing. Generation now records the complete model request, transcript cutoff, bound inputs and attempt count in its checkpoint before dispatch and replays that request on recovery. `TaskDriver::cancel_turn` drives abort cleanup for turn members that never dispatched, so a cancelled chain closes its exchange and releases the foreground slot without a manual drive. Also fixed: a submission can no longer replay onto an input that was only admitted, a rejected duplicate tool registration no longer replaces the registered tool, and `TaskDriver::changed` now subscribes when it waits instead of resolving against a commit that predates the call. `tests/k5_generation.rs` covers the chain with deterministic successor waits, tool recovery with and without retry safety, frozen-request recovery, cancelled-exchange closure followed by a provider-safe later turn, replay refusal, and registration rejection; `tests/k3_turns.rs` and `tests/k4_reads.rs` cover automatic pending-turn cleanup and the `changed` wait. Format, strict workspace Clippy and the full workspace test suite pass. Not established by these tests: end-to-end proof that recovery ignores divergent live transcript state, because no client-visible transcript append exists yet to create the divergence while a task is running; the freeze is covered by the checkpoint assertion and the attempt accounting.

2026-09-13: `TaskDriver::{create, open, create_with_capacity, open_with_capacity}` now open per-session SQLite stores, so a client does not construct a `Session` directly. `tests/k4_sqlite.rs::a_driver_creates_and_opens_an_on_disk_session` drives a turn over a freshly created database, closes it, reopens through the driver, and shows that opening reads only: the snapshot and summary are identical, the pending scoped successor is still pending, and the foreground slot is unchanged.

2026-09-13: K5 input admission and idle scheduling implemented, and the earlier replay refusal superseded. Admission now applies the `DESIGN.md` §11 mode/state table in one commit: submit on an idle conversation opens the turn that answers it and binds the input; steering or following up on a busy conversation queues the input instead of failing; queue-only and notice input is retained without waking the conversation; and submit on a busy conversation still admits nothing. `TaskDriver::admit_input` uses a configured `TurnTemplate`, `TaskDriver::submit_input` takes an explicit turn request, and `Session::queue_input` is the queue-only primitive, so the scheduler branches on no built-in name. Queued input is drained by the settlement that releases a conversation's turn slot — one input per successor turn, in admission order, no coordinator task — and `TaskDriver::schedule_next_turn` exposes that step for a conversation reopened with durable queued input. The previous refusal to replay a submission onto a queued input was correct only while submissions always opened turns; with queueing a legitimate outcome, a replay now reports the input as queued instead of inventing a turn. `tests/k5_idle.rs` covers a queued follow-up starting the next turn, deferred steering, queue-only/notice never starting a turn, submit-while-busy refusal, the missing-template refusal, opt-in scheduling, an explicit drain, and an end-to-end queued follow-up running the generation chain again; `session::tests::admission_policy_follows_mode_and_conversation_state` pins the whole policy table. Format, strict workspace Clippy and the full workspace test suite pass. Not established: mid-turn injection (a steer is delivered at the next turn boundary) and any per-conversation paused state.

2026-09-13: Idle-scheduling review fixes. An independently reviewed pass over the new admission/idle slice found four defects, all fixed. Scheduling is now scoped to the conversations whose foreground slot the settlement actually released (the settlement reports them), so a background or unrelated settlement cannot start queued work and a turn member in another conversation releases the slot owner's queue correctly. The `TurnTemplate` is shared configuration rather than per-handle state, so a clone cannot disagree about automatic turn shape. Scheduling reselects after a candidate was answered by someone else instead of giving up on the whole queue, and it refuses to bind input to a turn whose kind the driver cannot run, leaving the input queued for when the implementation is registered rather than assigning it to work that would settle `Unsupported`. `tests/k5_idle.rs` now also covers a settlement that released no slot, a cross-conversation turn member releasing the slot owner, shared configuration across clones, an invalidated candidate falling through to the next, an unregistered turn kind leaving input queued, a follow-up queued during cancellation, and resuming durable queued input across a SQLite close/open. The interleaving inside the select-then-bind window is not deterministically forced by a test; the reselect contract is.

2026-09-13: K6 ownership mechanism. `TaskPlan` can create owned conversations atomically with the outcome that created them. `TaskPlan::create_conversation` returns a plan-local handle; `PlannedEntry` and `PlannedTask` now take a `PlannedTarget` (an existing conversation or one the plan creates), mirroring `TaskDependency`, and the writer creates planned conversations first, owned by the settling task with the reciprocal ownership edge, before entries, input bindings and successors. Context seed is explicit: `PlannedConversation::fresh`, or `inherited` from an existing conversation at a cutoff validated by the same fork-safe-cutoff rule as any other created conversation. `MAX_PLAN_CONVERSATIONS` joins the enforced plan bounds, and `SessionError::PlanTooLarge` now reports the conversation count. `tests/k6_workers.rs` covers a fresh worker with an owned edge, its seeded brief and retained (background) task, an inherited worker seeing exactly its cutoff prefix, a foreign plan handle refused with full rollback, an invisible cutoff refused, the conversation bound, and a worker conversation surviving SQLite close/open with nothing started on open. Format, strict workspace Clippy and the full workspace test suite pass. Not established: any production worker entry point (the mechanism is exercised by test kinds), reuse of a retained worker, nested ownership limits, and retirement of worker history or scratch artifacts — "scratch retirement" in DESIGN still has no defined semantics, so it was deliberately not guessed at.

2026-09-13: K6 production retained spawn. `builtin::worker` registers a `worker` task kind whose immutable input is a `WorkerSpec` — a brief and an optional seed (`fresh`, or `inherited` at a supplied cutoff) — and whose settlement plans the owned conversation, the brief as a transcript entry carrying a user projection, and the worker's initial generation as a background task, all in the commit that settles the spawn. The brief is deliberately an entry rather than an admitted input: there is no receipt to replay and no input to consume, and `DESIGN.md` §11 allows a task with no assigned input to read its transcript. Reads gained `TaskDriver::{conversation, owned_conversations}`, so a client discovers the worker from its creator's durable ownership edge without materializing the session. `tests/k6_workers.rs` covers the real chain (the scripted model receives exactly the brief; an inherited worker receives exactly the cutoff prefix plus the brief), a cancelled spawn creating nothing, an interrupted spawn leaving no worker with recovery creating exactly one across a fault close and SQLite reopen, and absent reads for unknown ids. Format, strict workspace Clippy and the full workspace test suite pass. Deferred with reasons recorded in DESIGN §12 and the K6 section: joined runs (a dependency carrying the worker's final result is needed; a generation settles before its tools and continuation, so waiting on it is not a joined barrier), worker-local turn scope (without it a follow-up could overlap a running worker), the send/inspect/wait command surface, per-run interruption, reuse, nested ownership limits, and retirement — whose semantics remain undecided between archival, context retirement and artifact reclamation.

2026-09-13: K6 worker retirement implemented as a read-only archive. `Conversation` gained a durable `retired` flag (SQLite schema version 2; version-1 development databases are refused rather than migrated, and `tests/k6_retirement.rs` pins that refusal). `Session::{retire_conversation, reactivate_conversation}` and the driver's pair commit it. Retirement requires quiescence — an owned conversation with no foreground turn and no non-terminal task — and is idempotent, so a rejected retirement changes nothing. Every writer path rejects a retired conversation: transcript entries, task creation, input admission, opening a foreground turn and settlement plans, the last of which is covered by a plan whose failure rolls back the settlement and leaves the settling task recoverable. History, ownership, terminal outcomes, checkpoints and fork-visible prefixes are preserved and stay readable, input that was queued but never started is cancelled in the same commit, and reactivation clears the flag and starts nothing. Format, strict workspace Clippy and the full workspace test suite pass. Still deferred with reasons recorded: joined runs, worker-local turn scope, the send/inspect/wait command surface, per-run interruption, retained-worker reuse, nested ownership limits, artifact reclamation and deleting history — retirement archives, it does not delete.

2026-09-13: K6 worker-local turn scope. Planned successors now name their turn: `PlannedTurn::{Inherit, Own, Background}` replaced the `background` boolean, and the writer validates `Own` against the same one-authoritative-foreground-turn rule as any other turn, opening the slot in the same commit that creates the successor that roots it and rolling the whole plan back when the target conversation already holds one. `builtin::worker` uses `Own`, so a spawned worker's run occupies its own conversation's turn while the creator's conversation goes idle as soon as the spawn settles. Consequences, all pinned by `tests/k6_worker_turn.rs`: a follow-up admitted to a busy worker queues and drains into its own successor turn once the first chain finishes, so the two runs are ordered rather than overlapping; `TaskDriver::cancel_turn` on the worker's root stops exactly that run and leaves a sibling worker and the creator untouched; and a plan that tries to open a held slot is rejected with the settling task left recoverable. Successors that inherit or open a turn are born cancelled when the settling task's turn was cancelled, so abort cleanup still cannot smuggle runnable work into a stopped turn, while `Background` remains outside cancellation scope. Format, strict workspace Clippy and the full workspace test suite pass. Still deferred with reasons in DESIGN §12: joined runs (a worker now owns its turn, so the creator needs a result-carrying dependency rather than a wait), the send/inspect/wait command surface, per-run interruption as a first-class operation, reuse, nested ownership limits, and all reclamation beyond retirement.

2026-09-13: K6 durable turn completion. A turn root now records `turn_closed_by` — the member whose settlement closed the turn — written in the same commit as that settlement and the slot release, so a released slot never appears without the receipt naming what ended the turn. `TaskDriver::{turn_closed_by, wait_turn}` expose it, and `wait_turn` is the client-side "the worker's whole chain answered" wait, deliberately distinct from waiting for a worker's initial generation, because a generation settles as soon as it has planned its children. SQLite schema moved 2 -> 3 with a `tasks.turn_closed_by` column; version 1 and 2 development databases are refused rather than migrated. `tests/k6_turn_completion.rs` covers the receipt naming the closing member while the root keeps its own outcome, a later turn not reopening an earlier receipt, one turn's wait not blocking another worker's chain, a cancelled turn still recording its closure (through cleanup, not the mark), the non-turn-root refusal, and reopen. Format, strict workspace Clippy and the full workspace test suite pass. Reviewed by Astra, which replaced my initial conversation-quiescence design: the turn root is known when the worker is planned, it identifies *which run* produced a result (a follow-up turn cannot satisfy an old join), it keeps one result per dependency, and the writer already computes that completion. Astra also identified the cycle that a task-dependency on a turn introduces, which the next slice must reject.
