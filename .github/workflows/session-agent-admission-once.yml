name: Durable session agent admission once

on:
  push:
    branches: [main]
    paths:
      - .github/session-agent-admission.py
      - .github/session-agent-admission-owner.py
      - .github/workflows/session-agent-admission-once.yml

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.98.0
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Apply durable admission slice
        run: |
          python .github/session-agent-admission.py
          python .github/session-agent-admission-owner.py
      - run: cargo fmt
      - run: cargo fmt --check
      - run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
      - run: cargo test --locked --workspace
      - name: Commit validated slice
        run: |
          rm .github/session-agent-admission.py .github/session-agent-admission-owner.py .github/workflows/session-agent-admission-once.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add crates/ion-core/src/store/schema.rs crates/ion-core/src/store/mod.rs crates/ion-core/src/store/sql.rs crates/ion-core/src/delegate.rs crates/ion-core/src/tests/agent_store.rs crates/ion-core/src/tests/agent_family.rs .github/session-agent-admission.py .github/session-agent-admission-owner.py .github/workflows/session-agent-admission-once.yml
          git commit -m "refactor(agent): admit separate sessions atomically"
          git push origin HEAD:main
