name: Family durable observation once

on:
  push:
    branches: [main]
    paths:
      - .github/family-observation-patch.py
      - .github/family-observation-prepatch.py
      - .github/workflows/family-observation-once.yml

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.98.0
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Apply family observation slice
        run: |
          python .github/family-observation-prepatch.py
          python .github/family-observation-patch.py
      - run: cargo fmt
      - run: cargo fmt --check
      - run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
      - run: cargo test --locked --workspace
      - name: Commit validated slice
        run: |
          # No branch writes occur after this workflow is triggered, so this
          # validated source commit can fast-forward main directly.
          rm .github/family-observation-patch.py .github/family-observation-prepatch.py .github/workflows/family-observation-once.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add crates/ion-core/src/store/mod.rs crates/ion-core/src/store/sql.rs crates/ion-core/src/agent.rs crates/ion-core/src/delegate.rs crates/ion-core/src/tests/agent_family.rs .github/family-observation-patch.py .github/family-observation-prepatch.py .github/workflows/family-observation-once.yml
          git commit -m "refactor(agent): centralize durable observation"
          git push origin HEAD:main
