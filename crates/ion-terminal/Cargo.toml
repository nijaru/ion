[package]
name = "ion-terminal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Ion terminal lifecycle, input, capabilities, and renderer"

[dependencies]
crossterm = { version = "0.29.0", features = ["event-stream"] }
futures-util = "0.3.34"
ratatui = "0.30.2"

[dev-dependencies]
vt100 = "0.16.2"

[lints]
workspace = true
